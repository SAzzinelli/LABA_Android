package com.laba.firenze

/**
 * Costanti di configurazione allineate a iOS.
 * Quando false, la selezione gruppo (A/B) non è mostrata in Profilo e non filtra lezioni.
 */
object LabaConfig {
    const val USE_GROUP_FILTER = false
}
